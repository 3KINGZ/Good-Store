import React from "react";
import "./Input.scss";

const Input = () => {
  return (
    <div className="input-container">
      <input type="email" />
      <button>Subscribe</button>
    </div>
  );
};

export default Input;
